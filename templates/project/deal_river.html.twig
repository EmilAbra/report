{% extends 'base_proj.html.twig' %}

{% block title %}Casino Hold’em{% endblock %}

{% block content %}

<div class="holdem-wrapper">
    {{ include('/project/_holdem_header.html.twig') }}
    {{ include('/project/_ai_info.html.twig') }}

    <div class="holdem-table">
        <div class="cards-wrapper">
            <div class="ai-cards">
                <div class="playingCards faceImages game21">
                    <ul class="ai-table">
                        {% for card in aiPlayerHand|slice(0, 2) %}
                        <li>
                            <div class="card back">*</div>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
            <div class="table-cards">
                <div class="playingCards faceImages game21">
                    <ul class="table">
                        {% for card in boardCards %}
                        <li>
                            <div class="card rank-{{ card.rank }} {{ card.suit }}">
                                <span class="rank">{{ card.rank }}</span>
                                <span class="suit">&{{ card.suit }};</span>
                            </div>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
            <div class="result">
                {% for handValue, rank in playerHandValue %}
                    <p>Du fick {{ handValue }}!</p>
                {% endfor %}
            </div>
            <div class="player-cards">
                <div class="playingCards faceImages game21">
                    <ul class="table">
                        {% for card in playerHand|slice(0, 2) %}
                        <li>
                            <div class="card rank-{{ card.rank }} {{ card.suit }}">
                                <span class="rank">{{ card.rank }}</span>
                                <span class="suit">&{{ card.suit }};</span>
                            </div>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
        {{ include('/project/_hand_odds.html.twig') }}
    </div>

    <div class="holdem-player">
        {{ include('/project/_player_info.html.twig') }}
        <div class="holdem-buttons">
            <form action="{{ path('show_ai_cards') }}">
                <input type="submit" value="Visa datorns kort"/>
            </form>
        </div>
    </div>
</div>
{% endblock %}
